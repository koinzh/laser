% 定义波导参数
w = 2; % 波导宽度
h = 1; % 波导高度
n1 = 2.61; % 介质折射率
n2 = 1.45; % 

% 计算截止厚度
lambda = 1.48e-6; % 波长
k0 = 2 * pi / lambda;
kc = k0 * sqrt(n^2 - 1); % 临界波矢
d_cutoff = h * sqrt(1 - (k0/kc)^2); % 截止厚度

% 输出结果
fprintf('截止厚度为 %.4f\n', d_cutoff);

